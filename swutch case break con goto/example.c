// switch case, break, continue, goto statement
/*
#include <stdio.h>


int main()
{
    int num;
    int a = 1;

    printf("Enter number: ");
    scanf ("%d",&num);

    switch (num)
    {
        case 1 + 6:
        printf("user enter 1 value");
        break;

        case 'A':
        printf("user enter 2 value");
        break;

        case 12:
        printf("user enter 3 value");
        break;

        case 4:
        printf("user enter 4 value");
        break;
    
        default:
        printf("user enter wrong value");
        break;
    }

    return 0;
}*/

  /*  // break -->
    for (int i = 0; i < 30; i++)
    {
        if (i == 15)
        {
            break;
        }

        printf("%d, ", i);
    }

   // continue -->
    while(i!=10)
    {
        printf("%d", i);
        continue;
        i++;
    }

    // goto -->
    void checkEvenOrNot(int num)
    {
        if (num % 2 == 0)
            // jump to even
            goto even;
        else
            // jump to odd
            goto odd;
  
    even:
        printf("%d is even", num);
        // return if even
        return;
    odd:
        printf("%d is odd", num);
    }
  */
